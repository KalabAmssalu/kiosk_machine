import KioskMemberForm from "@/components/module/LedgerForm/KioskForm";

type Props = {};

const page = (props: Props) => {
	return (
		<div>
			<KioskMemberForm />
		</div>
	);
};

export default page;
